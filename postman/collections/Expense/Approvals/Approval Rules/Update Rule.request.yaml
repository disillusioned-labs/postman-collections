$kind: http-request
url: "{{base_url}}{{expense_port}}/api/v1/approval-rules/{{rule_id}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "step": 2
    }
order: 3000
