$kind: http-request
url: "{{base_url}}{{expense_port}}/api/v1/transactions/"
method: GET
description: |-
  Filters: status, project_id, created_by, category, date_from, date_to (RFC3339), limit, offset.
order: 1000
