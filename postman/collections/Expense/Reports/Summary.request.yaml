$kind: http-request
url: "{{base_url}}{{expense_port}}/api/v1/reports/summary"
method: GET
description: |-
  Filters: project_id, date_from, date_to (RFC3339). Org-wide for owner/admin;
  placement-scoped for everyone else.
order: 1000
