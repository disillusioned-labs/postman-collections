$kind: http-request
url: "{{base_url}}{{gateway_port}}/readyz"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Readiness succeeded", () => pm.response.code < 300);
    language: text/javascript
order: 2000
