$kind: http-request
url: "{{base_url}}{{gateway_port}}/healthz"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: |-
      pm.test("Liveness succeeded", () => pm.response.code < 300);
    language: text/javascript
order: 1000
