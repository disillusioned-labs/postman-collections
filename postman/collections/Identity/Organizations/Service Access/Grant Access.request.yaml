$kind: http-request
url: "{{base_url}}{{identity_port}}/api/v1/organizations/{{organization_id}}/service-access/{{user_id}}"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "service_name": "expense"
    }
description: Grant a user access to a service. Owner/admin implicitly have access to all services.
order: 1000
