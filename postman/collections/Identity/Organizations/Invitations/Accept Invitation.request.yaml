$kind: http-request
url: "{{base_url}}{{identity_port}}/api/v1/organizations/{{organization_id}}/invitations/{{invitation_id}}/accept"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: "{}"
order: 5000
